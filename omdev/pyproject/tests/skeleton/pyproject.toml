[build-system]
requires = [
    'setuptools',
]
build-backend = 'setuptools.build_meta'


##

[project]
name = 'omdevthing'


##

[tool.om.pyproject.srcs]
main = ['omdevthing']

[tool.o.pyproject.venvs]
default = { interp = '3.13.6', requires = ['-rrequirements-dev.txt'], srcs = ['@main'] }
